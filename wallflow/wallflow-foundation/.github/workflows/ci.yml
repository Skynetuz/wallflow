name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  lint-and-test-linux:
    name: Linux – fmt, check, clippy, test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Check workspace
        run: cargo check --workspace

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Run tests
        run: cargo test --workspace

  check-windows:
    name: Windows – check & test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Check workspace
        run: cargo check --workspace

      - name: Run tests
        run: cargo test --workspace

      # NOTE: Desktop attach smoke tests are NOT run in CI because the
      # Windows runner does not have an interactive Explorer desktop session.
      # Those tests require REQUIRES_REAL_WINDOWS_VALIDATION.

  ipc-smoke:
    name: IPC supervisor smoke
    runs-on: ubuntu-latest
    needs: lint-and-test-linux
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Build workspace
        run: cargo build -p wallflow-renderer -p wallflow-cli

      - name: IPC supervisor smoke test
        run: |
          cargo run -p wallflow-cli -- ipc-supervisor-smoke --timeout-secs 10 --heartbeat-interval-ms 500
          echo "IPC supervisor smoke test completed"

      - name: Apply static smoke test
        run: |
          cargo run -p wallflow-cli -- apply-static-smoke --timeout-secs 10 --heartbeat-interval-ms 500
          echo "Apply static smoke test completed"

      - name: Render sim smoke test
        run: |
          cargo run -p wallflow-cli -- render-sim-smoke --timeout-secs 5 --width 800 --height 450
          echo "Render sim smoke test completed"

      - name: Legacy headless heartbeat smoke
        run: |
          timeout 15 ./target/debug/wallflow-renderer --headless-heartbeat --heartbeat-interval-ms 500 --timeout-secs 5 || true
          echo "Headless heartbeat test completed"
