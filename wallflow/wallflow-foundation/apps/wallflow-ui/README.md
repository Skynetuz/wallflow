# WallFlow UI placeholder

The UI should be implemented after Core/Renderer foundations compile.

Planned stack:

- Tauri 2
- React
- TypeScript
- minimal capabilities

Rule: UI must call Core commands only. It must not own renderer process state.
